name: lambda
version: 0.0.0.0

dependencies:
  - base
  - text
  - containers
  - array
  - transformers
  - mtl
  - megaparsec

default-extensions:
  - NoFieldSelectors

library:
  source-dirs: src
  ghc-options: -Wall

tests:
  test:
    main: Main
    source-dirs:
      - test
      - src
    dependencies:
      - tasty
      - tasty-hunit
